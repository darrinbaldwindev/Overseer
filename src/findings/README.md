# Findings Layer

The findings layer will normalize evidence-backed observations and apply the finding lifecycle protocol.

Responsibilities:

- stable finding identity;
- candidate validation;
- status transitions;
- severity/confidence handling;
- deduplication;
- historical event recording.

It must never turn unsupported inference into a confirmed finding.
